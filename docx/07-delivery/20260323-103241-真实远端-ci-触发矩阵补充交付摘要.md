# 真实远端 CI 触发矩阵补充交付摘要

- 阶段：07-delivery
- 提交时间：20260323-103241
- 责任角色：主代理
- 当前状态：done
- 评审状态：approved
- 输入依据：
  - docx/06-test/20260323-103241-真实远端-ci-触发矩阵补充测试报告.md

## 目标

1. 固化“真实远端 CI 触发矩阵补充验证”的最终结果。
2. 把 `push / workflow_dispatch / pull_request` 的真实成功记录写入仓库长期文档。

## 核心内容

## 交付结果

1. 当前 GitHub Actions workflow 已在以下触发方式下获得真实成功记录：
   - `push -> main`
   - `push -> branch`
   - `workflow_dispatch`
   - `pull_request`
2. 关键 run 证据：
   - `#4` `push/main`：`https://github.com/jkjk02/ling-shu-tai/actions/runs/23417193087`
   - `#5` `push/branch`：`https://github.com/jkjk02/ling-shu-tai/actions/runs/23417292873`
   - `#6` `workflow_dispatch`：`https://github.com/jkjk02/ling-shu-tai/actions/runs/23418481414`
   - `#8` `pull_request`：`https://github.com/jkjk02/ling-shu-tai/actions/runs/23418512823`
3. `test-results` 与 `playwright-report` artifact 在补充验证中持续可用。
4. 临时验证 PR `#1` 已关闭，临时分支 `ci-pr-validation` 与 `ci-branch-validation` 已删除，仓库已回到干净状态。

## 操作说明书

### 常见操作

1. 正常提交到 `main` 或功能分支会触发 CI。
2. 需要手动触发时，可在 GitHub Actions 页面使用 `workflow_dispatch`。
3. 创建 PR 时，`pull_request` 事件会自动触发同一条 CI workflow。

### 验证方式

1. 本地近似仿真：`CI=1 npm run verify`
2. 远端真实验证：
   - `push`
   - `workflow_dispatch`
   - `pull_request`

### 已知限制

1. 当前只验证了单 job workflow，不包含矩阵或多平台 runner。
2. 当前未专门注入失败场景验证 artifact 在失败态下的可用性，但成功态 artifact 已持续存在。

## 风险与待确认项

1. 若后续调整 workflow 结构、依赖版本或 runner 平台，建议重新补一轮同样的触发矩阵验证。
2. 若后续启用分支保护规则，还需额外验证状态检查门禁配置。

## 交接输出

1. “真实远端 CI 落地”切片现已补齐主要触发矩阵的真实证据。
2. 后续若继续推进，优先级应转向失败场景验证、并发取消验证或状态检查门禁配置。

## 批准记录

- 评审人：主代理
- 评审结论：approved
- 批准时间：20260323-103800
- 备注：补充交付摘要已记录主要真实触发矩阵证据。
