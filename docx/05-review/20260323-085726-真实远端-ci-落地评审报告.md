# 真实远端 CI 落地评审报告

- 阶段：05-review
- 提交时间：20260323-085726
- 责任角色：implementation_reviewer
- 当前状态：done
- 评审状态：approved
- 输入依据：
  - docx/04-implementation/20260323-085726-真实远端-ci-落地实现记录.md
  - docx/03-plan/20260323-083945-真实远端-ci-落地实施计划.md

## 目标

1. 检查实现是否严格落在“真实远端 CI 落地”切片范围内。
2. 判断是否允许进入测试阶段。

## 核心内容

1. 评审结论：通过。
2. 主要依据：
   - 改动集中在 [ci.yml](/home/lst/ling-shu-tai/.github/workflows/ci.yml)、[playwright.smoke.config.ts](/home/lst/ling-shu-tai/playwright.smoke.config.ts)、[README.md](/home/lst/ling-shu-tai/README.md) 与 `docx/`，没有扩散到业务代码。
   - 远端 workflow 仍复用 `npm run verify` 作为主入口，没有在 CI 中重写独立业务验证逻辑。
   - 新增 `workflow_dispatch`、最小权限、并发控制、超时、pip cache 和 Playwright `--with-deps`，对远端 runner 稳定性有直接增益。
   - 失败后可上传 `test-results/` 和 `playwright-report/`，诊断能力比原骨架明显提升。
   - 在实现阶段已识别并修复 `video` 依赖 `ffmpeg` 的问题，说明 CI 仿真不是只做静态修改。
3. 残余风险：
   - 本会话未直接推送远端或触发 GitHub run，首次真实远端运行仍需用户在 GitHub 上执行。
   - artifact 使用 `if: always()`，成功场景也会上传报告，后续可按存储成本再做收敛。

## 风险与待确认项

1. GitHub hosted runner 的镜像演进仍可能影响 Playwright 依赖或系统包行为。
2. 本轮没有扩展多 job 或矩阵，因此失败定位已明显改善，但运行时长不是最优。

## 交接输出

1. 允许进入 `06-test`。
2. 测试阶段应把“本地 CI 仿真通过”和“远端待用户触发”明确区分记录。

## 批准记录

- 评审人：implementation_reviewer / 主代理
- 评审结论：approved
- 批准时间：20260323-090400
- 备注：实现符合计划边界，允许进入测试阶段。
