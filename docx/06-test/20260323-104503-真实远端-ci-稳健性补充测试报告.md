# 真实远端 CI 稳健性补充测试报告

- 阶段：06-test
- 提交时间：20260323-104503
- 责任角色：tester
- 当前状态：done
- 评审状态：approved
- 输入依据：
  - docx/07-delivery/20260323-103241-真实远端-ci-触发矩阵补充交付摘要.md

## 目标

1. 补齐真实远端 CI 的失败态 artifact 验证。
2. 补齐同分支并发提交时的 concurrency 取消验证。

## 核心内容

## 测试范围

1. 失败态 artifact 上传验证
2. 同分支并发取消验证

## 测试结论

1. 轮次：1
2. 场景：真实远端 CI 稳健性补充验证
3. 结果：passed

## 执行结果

1. 失败态 artifact 上传验证
   - 临时分支：`ci-failure-validation`
   - commit：`b2aa854b61d59c9f1a9057dfc420718d4992a9aa`
   - run number：`10`
   - run id：`23419076747`
   - run 结果：`failure`
   - 失败点：`Run unified verification`
   - artifact 上传结果：
     - `test-results-1` 已上传
     - `playwright-report-1` 已上传
   - 结论：`if: always()` 在失败态下有效
2. 同分支并发取消验证
   - 临时分支：`ci-concurrency-validation`
   - 被取消 run：
     - run number：`11`
     - run id：`23419144560`
     - commit：`d47e7b3ea2a1d8af6b8e28d3192401b45f876717`
     - 结果：`cancelled`
   - 取代后成功 run：
     - run number：`12`
     - run id：`23419155335`
     - commit：`524860af48437e2975b0cc20685328d8e5dc3c36`
     - 结果：`success`
     - artifact：
       - `test-results-1`
       - `playwright-report-1`
   - 结论：同分支新 run 会取消旧 run，concurrency 配置生效
3. 清理结果：
   - `ci-failure-validation` 已删除
   - `ci-concurrency-validation` 已删除
   - 仓库已切回 `main`

## 错误日志

~~~text
Failure-path validation
- run #10
- branch: ci-failure-validation
- conclusion: failure
- step 9 "Run unified verification": failure
- step 10/11 artifacts still uploaded

Concurrency validation
- run #11 on ci-concurrency-validation: cancelled
- run #12 on ci-concurrency-validation: success
~~~

## 回流动作

- 回流给：无
- 修复负责人：无
- 下一轮计划：进入稳健性交付摘要，固化失败态与并发验证结论

## 风险与待确认项

1. 当前已验证失败态 artifact 和并发取消，但仍未覆盖手动 rerun、矩阵 job、受保护分支门禁等行为。
2. 失败态验证使用的是临时故障注入分支，不影响 `main`，但后续若变更 workflow 结构仍需重验。

## 交接输出

1. 允许进入 `07-delivery` 稳健性补充交付摘要。
2. 当前可以明确宣告：
   - 成功触发矩阵已验证
   - 失败态 artifact 已验证
   - 同分支并发取消已验证

## 批准记录

- 评审人：tester / 主代理
- 评审结论：approved
- 批准时间：20260323-104900
- 备注：真实远端 CI 稳健性补充验证完成。
