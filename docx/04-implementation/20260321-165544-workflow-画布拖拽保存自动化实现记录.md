# Workflow 画布拖拽保存自动化实现记录

- 阶段：04-implementation
- 提交时间：20260321-165544
- 责任角色：systems_implementer
- 当前状态：done
- 评审状态：pending
- 输入依据：
  - docx/03-plan/20260321-163007-workflow-画布拖拽保存自动化实施计划.md

## 目标

1. 落地 Workflow 画布拖拽/保存最小浏览器自动化闭环。
2. 让拖拽后的持久化结果通过真实浏览器和真实后端得到自动验证。

## 核心内容

1. 实现文件：
   - [WorkflowsPage.vue](/home/lst/ling-shu-tai/src/pages/WorkflowsPage.vue)
   - [workflow-canvas.spec.ts](/home/lst/ling-shu-tai/tests/browser/workflow-canvas.spec.ts)
   - [README.md](/home/lst/ling-shu-tai/README.md)
2. 主要实现点：
   - 为 Workflow 页面增加最小稳定测试锚点，包括新建、保存、删除、模板按钮、画布和节点属性。
   - 修复 `selectedWorkflowId` 为空时仍回灌第一条 Workflow 的 create mode 状态 bug。
   - 新增 Workflow 浏览器用例，覆盖：
     - 新建唯一命名 Workflow
     - 节点模板加点
     - 连线
     - 拖拽导致位置变化
     - 保存
     - 刷新后回读
     - 删除清理
   - Workflows 用例继续复用隔离数据目录启动真实后端，不污染默认 `backend/data`。
   - README 同步 Workflow 浏览器自动化覆盖边界。
3. 自检结果：
   - `npm run build` 通过
   - `npm run test:browser` 通过
   - `npm run verify` 通过

## 风险与待确认项

1. 当前浏览器验证仍只覆盖最小成功路径，未覆盖更复杂异常矩阵。
2. 表格 loading mask 和拖拽时序在无头环境中仍是自动化稳定性的主要风险点。

## 交接输出

1. 评审阶段应重点检查：
   - 是否真实经过浏览器拖拽与保存，而不是 API 替代。
   - 是否没有扩展到视觉回归、远端 CI 和执行引擎。
   - 是否修复了 Workflow 页面新建态逻辑缺陷。
2. 测试阶段需以 `npm run verify` 作为统一放行依据。

## 批准记录

- 评审人：
- 评审结论：
- 批准时间：
- 备注：
