# MCP 浏览器级真实提交自动化实现记录

- 阶段：04-implementation
- 提交时间：20260321-162046
- 责任角色：systems_implementer
- 当前状态：done
- 评审状态：pending
- 输入依据：
  - docx/03-plan/20260321-160856-mcp-浏览器级真实提交自动化实施计划.md

## 目标

1. 落地 managed MCP 浏览器级真实创建提交自动化。
2. 让浏览器测试通过隔离数据目录运行真实后端，避免污染默认数据。

## 核心内容

1. 实现概览：
   - 新增 [playwright_backend.py](/home/lst/ling-shu-tai/backend/tests/playwright_backend.py) 作为 Playwright 专用后端入口。
   - 更新 [playwright.smoke.config.ts](/home/lst/ling-shu-tai/playwright.smoke.config.ts)，把 `webServer` 后端命令切到隔离脚本。
   - 更新 [mcp-smoke.spec.ts](/home/lst/ling-shu-tai/tests/browser/mcp-smoke.spec.ts)，新增 managed MCP 真实创建、刷新后校验、详情校验、删除清理用例。
   - 更新 [README.md](/home/lst/ling-shu-tai/README.md)，说明浏览器测试已使用隔离数据目录并覆盖真实提交闭环。
2. 关键实现策略：
   - 启动时复制 `backend/data` 与浏览器测试 fixtures 到临时目录。
   - 通过环境变量覆盖 `LINGSHU_DATA_DIR` 与各 CLI fixture 根目录。
   - 浏览器用例保持真实 UI 操作，不使用 API 替代提交。
   - 删除清理与隔离目录双保险并存，确保重复执行不残留。
3. 代码归属：
   - 浏览器运行时隔离：主代理
   - 浏览器用例：主代理
   - 文档与说明：主代理

## 风险与待确认项

1. 本轮只覆盖 managed MCP 成功路径，未覆盖异常矩阵。
2. 当前浏览器验证仍依赖本地回环网络权限。

## 交接输出

1. 验证方式：
   - `npm run build`
   - `npm run test:browser`
   - `npm run verify`
2. 当前实现已准备进入 `05-review`。

## 批准记录

- 评审人：
- 评审结论：
- 批准时间：
- 备注：
