# 浏览器级 MCP 页面冒烟自动化实施计划

- 阶段：03-plan
- 提交时间：20260321-150010
- 责任角色：solution_architect
- 当前状态：approved
- 评审状态：approved
- 输入依据：
  - docx/02-architecture/20260321-150010-浏览器级-mcp-页面冒烟自动化架构方案.md

## 目标

1. 以最小改动交付 MCP 页面为重点的浏览器级冒烟自动化。

## 核心内容

## 并行拆分

| worker | 负责范围 | 写入边界 | 输入文档 | 交付物 |
| --- | --- | --- | --- | --- |
| 主代理 | Playwright 配置、测试脚本、脚本接线 | `playwright.smoke.config.ts` `tests/browser/` `package.json` | 已批准架构方案 | 浏览器冒烟入口 |
| 主代理 | 稳定选择器、README、CI、docx | `src/layouts/AppLayout.vue` `src/pages/McpsPage.vue` `README.md` `.github/workflows/ci.yml` `docx/` | 已批准架构方案 | 运行说明与 CI 骨架 |

1. 本轮任务量小，不再拆分额外 worker。

## 实施步骤

1. 安装 `@playwright/test`，复用本机已有 Chromium 缓存。
2. 新增 Playwright 配置和 MCP 页面冒烟用例。
3. 为导航和 MCP 新建按钮补少量稳定选择器。
4. 把 `test:browser` 接入 `verify`。
5. 更新 README 与 CI 骨架。

## 验证计划

1. `npm run build`
2. `npm run test:browser`
3. `npm run verify`

## 完成定义

1. 至少 2 条浏览器冒烟用例通过。
2. `npm run verify` 通过。
3. README 与 CI 骨架已同步。

## 风险与待确认项

1. 浏览器测试执行依赖本地端口和 Chromium 进程，运行权限不足会直接失败。

## 交接输出

1. 允许实施阶段直接落地。
2. 评审重点：
   - 依赖与配置是否最小化。
   - MCP 页面关键断言是否覆盖新切片价值。
   - CI 是否不再停留在只跑后端和构建。

## 批准记录

- 评审人：主代理
- 评审结论：approved
- 批准时间：20260321-150050
- 备注：计划锁定为最小 Playwright 冒烟接入。
