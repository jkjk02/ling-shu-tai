# Workflow 画布拖拽保存自动化实施计划

- 阶段：03-plan
- 提交时间：20260321-163007
- 责任角色：solution_architect
- 当前状态：approved
- 评审状态：approved
- 输入依据：
  - docx/02-architecture/20260321-163007-workflow-画布拖拽保存自动化架构方案.md

## 目标

1. 补齐 Workflow 画布浏览器层的最小业务闭环。
2. 让拖拽后的保存与刷新回读进入统一验证链。

## 核心内容

## 并行拆分

| worker | 负责范围 | 写入边界 | 输入文档 | 交付物 |
| --- | --- | --- | --- | --- |
| 主代理 | 页面可测性增强与状态修复 | `src/pages/WorkflowsPage.vue` | 已批准架构方案 | 稳定测试锚点与正确的新建态逻辑 |
| 主代理 | Workflow 浏览器闭环用例 | `tests/browser/workflow-canvas.spec.ts` | 已批准架构方案 | 新建、加点、连线、拖拽、保存、刷新回读、删除清理用例 |
| 主代理 | 验证与文档 | `README.md` `docx/` | 已批准架构方案 | 运行说明、实现记录、测试结果 |

1. 本轮代码改动耦合度高，不再拆分额外代码 worker。

## 实施步骤

1. 先在 Workflow 页面补稳定测试锚点。
2. 修正 create mode 状态逻辑，避免新建时回灌第一条 Workflow。
3. 新增 Workflow 画布浏览器自动化用例。
4. 跑 `npm run test:browser`，再跑 `npm run verify`。
5. 回填评审、测试和交付文档。

## 验证计划

1. `npm run build`
2. `npm run test:browser`
3. `npm run verify`

## 完成定义

1. 浏览器测试总数达到 `4/4`，其中新增 1 条 Workflow 画布闭环用例。
2. `npm run verify` 通过。
3. Workflow 页面新建态逻辑正确，不再回灌第一条 Workflow。
4. README 与交付文档已同步。

## 风险与待确认项

1. 拖拽与刷新回读的自动化等待条件若定义不准，容易出现假失败。
2. 表格刷新 loading mask 需要在浏览器测试中显式处理。

## 交接输出

1. 允许实施阶段直接落地。
2. 评审重点：
   - 是否真实经过浏览器拖拽与保存。
   - 是否没有扩展到视觉回归与异常矩阵。
   - 是否修正了 Workflow 页面新建态逻辑缺陷。

## 批准记录

- 评审人：主代理
- 评审结论：approved
- 批准时间：20260321-163300
- 备注：计划锁定为 Workflow 画布拖拽/保存最小浏览器自动化切片。
